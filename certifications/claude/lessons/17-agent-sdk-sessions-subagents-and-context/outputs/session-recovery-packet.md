# Session 恢复包：Client 迁移

## 目标与范围

迁移 HTTP client，同时不改变公开行为。范围限定为 `src/client.py` 和 `tests/test_client.py`；部署和外部写入均被阻止。

## 持久化状态

Manifest `migration-v2` 记录了当前分支、依赖版本、任务状态、文件 hash `4cc0-demo`、12 个通过的测试、一个因超时而受阻的测试用例以及产物 ID。对话历史不是权威信息源。

## 重新验证

下一次编辑前，重新验证分支、依赖行为、文件 hash、针对性测试和当前审批。依赖项发生变化会使旧计划失效。

## 副作用对账

Tool 调用 `write-018` 的结果未知。重试前比较持久化的 hash。复用幂等性 key `migration-client-v2-write-018`；在对账完成之前，绝不使用新的写入身份。

## Context 预算

百分之二十用于目标和约束，百分之二十用于当前 manifest 状态，百分之四十五用于与决策相关的证据，百分之十五用于输出契约。原始日志仍保留在产物引用之后。

## 独立审查

隔离的审查者接收 diff、需求、测试和 rubric。它返回附带证据的完整、部分完成或受阻状态，并且不接收实现 transcript。
