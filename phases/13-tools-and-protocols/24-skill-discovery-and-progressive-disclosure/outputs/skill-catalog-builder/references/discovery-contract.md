# 发现契约

发现分为三个披露级别：

1. 目录：读取路由所需的 `name`、`description`、作用域和路径。
2. 激活：在明确的大小预算内加载所选 SKILL.md 的正文。
3. 执行支持：仅在需要时加载直接指定的文件，例如 `references/schema.md`。

host 负责定义作用域位置、优先级、冲突行为和预算。目录构建器必须在其输出中清晰呈现这些选择。

可移植的一级引用是 Skill 目录中的常规文件，或其直接子目录中的文件。拒绝绝对路径、`..`、反斜杠、符号链接以及更深层的引用链。
