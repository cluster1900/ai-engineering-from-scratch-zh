# 评审评分标准

五个维度，评分为 0 到 2。

1. 问题匹配度 —— 这次变更是否解决了任务中陈述的问题？
2. 范围纪律 —— 编辑是否被限制在 contract 内？
3. 假设 —— 隐含假设是否已写明？
4. 验证质量 —— acceptance 是否真正证明了目标？
5. 交接准备度 —— 下一次 session 是否能干净地接手？

总分 >= 7 且没有 0 分：pass。总分 5-6：soft fail。低于 5 或任一维度为 0：hard fail。
